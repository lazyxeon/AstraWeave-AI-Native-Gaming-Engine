title: AstraWeave — AI-Native Game Engine
description: Production-ready, AI-native game engine built in Rust with zero human-written code
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme

markdown: kramdown
highlighter: rouge

exclude:
  - Cargo.toml
  - Cargo.lock
  - Makefile
  - target/
  - "*.rs"
