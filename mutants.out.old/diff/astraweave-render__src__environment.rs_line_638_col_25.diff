--- astraweave-render/src/environment.rs
+++ delete field mipmap_filter from struct wgpu::SamplerDescriptor expression in SkyRenderer::render
@@ -630,17 +630,17 @@
                 if let (Some(tex_pipeline), Some(tex_bgl)) =
                     (&self.texture_pipeline, &self.texture_bgl)
                 {
                     // Create sampler
                     let sampler = device.create_sampler(&wgpu::SamplerDescriptor {
                         label: Some("Skybox Sampler"),
                         mag_filter: wgpu::FilterMode::Linear,
                         min_filter: wgpu::FilterMode::Linear,
-                        mipmap_filter: wgpu::FilterMode::Linear,
+                         /* ~ changed by cargo-mutants ~ */
                         ..Default::default()
                     });
 
                     let bg = device.create_bind_group(&wgpu::BindGroupDescriptor {
                         label: Some("Sky Texture BG"),
                         layout: tex_bgl,
                         entries: &[
                             wgpu::BindGroupEntry {
