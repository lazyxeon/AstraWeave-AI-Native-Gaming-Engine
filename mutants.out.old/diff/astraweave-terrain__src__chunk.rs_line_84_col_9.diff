--- astraweave-terrain/src/chunk.rs
+++ replace TerrainChunk::id -> ChunkId with Default::default()
@@ -76,17 +76,17 @@
             heightmap,
             biome_map,
             mesh_dirty: true,
         }
     }
 
     /// Get the chunk ID
     pub fn id(&self) -> ChunkId {
-        self.id
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get the heightmap
     pub fn heightmap(&self) -> &Heightmap {
         &self.heightmap
     }
 
     /// Get the biome map
