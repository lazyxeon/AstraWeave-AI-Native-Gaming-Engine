--- astraweave-core/src/schema.rs
+++ replace - with / in <impl std::ops::Sub for IVec2>::sub
@@ -136,17 +136,17 @@
     }
 }
 
 impl std::ops::Sub for IVec2 {
     type Output = Self;
     fn sub(self, rhs: Self) -> Self {
         Self {
             x: self.x - rhs.x,
-            y: self.y - rhs.y,
+            y: self.y / /* ~ changed by cargo-mutants ~ */ rhs.y,
         }
     }
 }
 
 #[derive(Clone, Debug, Serialize, Deserialize)]
 /// A snapshot of the game world at a point in time, used for AI perception.
 ///
 /// `WorldSnapshot` is the primary input to all AI orchestrators. It captures
