--- astraweave-render/src/clustered_forward.rs
+++ replace ClusteredForwardRenderer::update_lights with ()
@@ -333,17 +333,17 @@
             light_indices: Vec::new(),
             _max_lights: max_lights,
             max_lights_per_cluster,
         }
     }
 
     /// Update lights for the current frame
     pub fn update_lights(&mut self, lights: Vec<GpuLight>) {
-        self.lights = lights;
+        () /* ~ changed by cargo-mutants ~ */
     }
 
     /// Build clusters and assign lights
     pub fn build_clusters(
         &mut self,
         queue: &wgpu::Queue,
         view_matrix: Mat4,
         _proj_matrix: Mat4,
