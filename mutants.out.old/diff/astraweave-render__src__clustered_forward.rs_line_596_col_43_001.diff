--- astraweave-render/src/clustered_forward.rs
+++ replace - with / in ClusteredForwardRenderer::light_intersects_cluster
@@ -588,17 +588,17 @@
             light_pos_view
                 .y
                 .clamp(cluster.min_bounds[1], cluster.max_bounds[1]),
             light_pos_view
                 .z
                 .clamp(cluster.min_bounds[2], cluster.max_bounds[2]),
         );
 
-        let distance_sq = (light_pos_view - closest_point).length_squared();
+        let distance_sq = (light_pos_view / /* ~ changed by cargo-mutants ~ */ closest_point).length_squared();
         distance_sq <= radius * radius
     }
 
     /// Get the bind group layout
     pub fn bind_group_layout(&self) -> &wgpu::BindGroupLayout {
         &self.cluster_bind_group_layout
     }
 
