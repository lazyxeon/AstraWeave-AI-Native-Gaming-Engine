--- astraweave-terrain/src/chunk.rs
+++ replace - with / in ChunkId::distance_to
@@ -48,17 +48,17 @@
             }
         }
 
         chunks
     }
 
     /// Calculate distance to another chunk (in chunk units)
     pub fn distance_to(self, other: ChunkId) -> f32 {
-        let dx = (self.x - other.x) as f32;
+        let dx = (self.x / /* ~ changed by cargo-mutants ~ */ other.x) as f32;
         let dz = (self.z - other.z) as f32;
         (dx * dx + dz * dz).sqrt()
     }
 }
 
 /// A single terrain chunk containing heightmap and biome data
 #[derive(Debug, Clone)]
 pub struct TerrainChunk {
