--- astraweave-render/src/camera.rs
+++ replace Camera::dir -> Vec3 with Default::default()
@@ -21,22 +21,17 @@
         Mat4::perspective_rh(self.fovy, self.aspect.max(0.01), self.znear, self.zfar)
     }
 
     pub fn vp(&self) -> Mat4 {
         self.proj_matrix() * self.view_matrix()
     }
 
     pub fn dir(yaw: f32, pitch: f32) -> Vec3 {
-        let cy = yaw.cos();
-        let sy = yaw.sin();
-        let cp = pitch.cos();
-        let sp = pitch.sin();
-        // Standard direction: positive pitch = look up
-        Vec3::new(cy * cp, sp, sy * cp).normalize()
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Debug, Clone, Copy)]
 pub enum CameraMode {
     FreeFly,
     Orbit,
 }
