--- astraweave-render/src/clustered_forward.rs
+++ replace ClusteredForwardRenderer::bind_group_layout -> &wgpu::BindGroupLayout with Box::leak(Box::new(Default::default()))
@@ -594,17 +594,17 @@
         );
 
         let distance_sq = (light_pos_view - closest_point).length_squared();
         distance_sq <= radius * radius
     }
 
     /// Get the bind group layout
     pub fn bind_group_layout(&self) -> &wgpu::BindGroupLayout {
-        &self.cluster_bind_group_layout
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get the bind group
     pub fn bind_group(&self) -> &wgpu::BindGroup {
         &self.cluster_bind_group
     }
 
     /// Get configuration
