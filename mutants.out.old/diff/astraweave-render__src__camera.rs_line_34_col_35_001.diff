--- astraweave-render/src/camera.rs
+++ replace * with / in Camera::dir
@@ -26,17 +26,17 @@
     }
 
     pub fn dir(yaw: f32, pitch: f32) -> Vec3 {
         let cy = yaw.cos();
         let sy = yaw.sin();
         let cp = pitch.cos();
         let sp = pitch.sin();
         // Standard direction: positive pitch = look up
-        Vec3::new(cy * cp, sp, sy * cp).normalize()
+        Vec3::new(cy * cp, sp, sy / /* ~ changed by cargo-mutants ~ */ cp).normalize()
     }
 }
 
 #[derive(Debug, Clone, Copy)]
 pub enum CameraMode {
     FreeFly,
     Orbit,
 }
